export type Farmer = {
  farmerId: string;
  firstName: string;
  lastName: string;
  region: string;
  district: string;
  contactNumber: string;
  registrationDate: string;
  productsPurchased: string[];
};